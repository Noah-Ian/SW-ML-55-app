version: "3.9"

services:
  backend:
    build: ./backend
    container_name: soilsense-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: .
    container_name: soilsense-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
